{
  "name": "starium-orchestra",
  "private": true,
  "scripts": {
    "lint": "pnpm -r --parallel run lint",
    "typecheck": "pnpm -r run typecheck",
    "build": "pnpm -r run build",
    "test": "node scripts/run-tests-with-recap.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.14.2"
}
